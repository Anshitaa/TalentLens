apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: talentlens
spec:
  selector:
    app: mlflow
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500   # access via minikube ip:30500
